# Memory System v2.3 Design Spec

> 补齐JSONL持久化 + 冲突策略完善，让代码完全匹配STAR描述

**Goal:** 补齐记忆管理系统缺失的两项功能：
1. JSONL持久化：语义记忆写入时同步备份到JSONL文件
2. 冲突策略完善：增加OVERWRITE和CLEAR两种策略

**Architecture:**
- JSONL备份：同步写入，不阻塞主流程，失败只warning
- OVERWRITE：LLM判定触发，完全替换旧记忆
- CLEAR：LLM判定触发，批量清除某类记忆

---

## 1. JSONL持久化

### 1.1 实现位置

新建文件：`backend/app/core/memory/semantic_backup.py`

### 1.2 核心类设计

```python
@dataclass
class SemanticBackupConfig:
    """JSONL backup configuration."""
    enabled: bool = True
    backup_path: str = "./data/semantic_backup.jsonl"
    max_file_size_mb: float = 100.0  # 超过此大小触发rotate


class SemanticJSONLBackup:
    """JSONL backup for semantic memories.

    Writes each semantic memory to a JSONL file as backup.
    Synchronous write, non-blocking on failure (only warning).
    """

    def __init__(self, config: SemanticBackupConfig):
        self._config = config
        self._ensure_directory()

    def append(self, memory: MemoryItem) -> bool:
        """Append a memory to JSONL file.

        Args:
            memory: MemoryItem to backup

        Returns:
            True if written successfully, False otherwise
        """
        if not self._config.enabled:
            return True

        try:
            line = json.dumps(memory.to_dict(), ensure_ascii=False)
            with open(self._config.backup_path, "a", encoding="utf-8") as f:
                f.write(line + "\n")
            return True
        except Exception as e:
            logger.warning(f"[SemanticBackup] Write failed: {e}")
            return False

    def read_all(self) -> List[MemoryItem]:
        """Read all memories from backup file.

        Used for recovery when ChromaDB data lost.
        """
        if not os.path.exists(self._config.backup_path):
            return []

        memories = []
        with open(self._config.backup_path, "r", encoding="utf-8") as f:
            for line in f:
                try:
                    data = json.loads(line.strip())
                    memories.append(MemoryItem.from_dict(data))
                except json.JSONDecodeError:
                    continue
        return memories

    def get_stats(self) -> dict:
        """Get backup file statistics."""
        if not os.path.exists(self._config.backup_path):
            return {"exists": False, "count": 0}

        size_mb = os.path.getsize(self._config.backup_path) / (1024 * 1024)
        count = sum(1 for _ in open(self._config.backup_path, "r", encoding="utf-8"))
        return {
            "exists": True,
            "path": self._config.backup_path,
            "size_mb": round(size_mb, 2),
            "count": count,
            "max_size_mb": self._config.max_file_size_mb,
        }
```

### 1.3 集成点

**修改 `backend/app/core/memory/retrieval.py` 或 `semantic.py`：**

在 `SemanticMemory.add_memory()` 或类似写入方法中，成功写入ChromaDB后调用备份：

```python
async def add_memory(self, user_id: str, content: str, memory_type: str, metadata: dict) -> str:
    # ... ChromaDB写入逻辑
    memory_id = collection.add(...)

    # 同步JSONL备份
    if self._backup and self._backup_config.enabled:
        memory_item = MemoryItem(
            content=content,
            level=MemoryLevel.SEMANTIC,
            memory_type=MemoryType(memory_type),
            metadata=metadata,
        )
        self._backup.append(memory_item)

    return memory_id
```

### 1.4 配置扩展

**修改 `backend/app/core/memory/config.py`：**

```python
@dataclass
class MemoryConfig:
    # ... existing config

    # JSONL backup
    jsonl_backup_enabled: bool = True
    jsonl_backup_path: str = "./data/semantic_backup.jsonl"
    jsonl_max_file_size_mb: float = 100.0
```

### 1.5 数据格式

每行一个JSON对象：

```json
{
  "id": "abc123",
  "content": "用户喜欢自然风光",
  "level": "semantic",
  "memory_type": "preference",
  "metadata": {
    "user_id": "user001",
    "strength": {"initial_strength": 0.8, "access_count": 0}
  },
  "confidence": 0.8,
  "importance": 0.85,
  "created_at": "2026-04-14T10:30:00"
}
```

---

## 2. 冲突策略完善

### 2.1 操作类型扩展

**修改 `backend/app/core/memory/conflict_resolver.py`：**

```python
class MemoryOperation(Enum):
    """Memory operation types."""
    ADD = "add"         # 全新信息，无相似记忆
    UPDATE = "update"   # 同类信息merge（保留metadata）
    OVERWRITE = "overwrite"  # 完全替换（删除旧记忆，新建）
    DELETE = "delete"         # 单条记忆失效
    CLEAR = "clear"           # 批量清除某类记忆
    NOOP = "noop"             # 重复/忽略
```

### 2.2 LLM Prompt更新

```python
CONFLICT_PROMPT = """
判断以下两句话的关系：

旧信息: {existing_content}
新信息: {new_content}

请回答以下选项之一：
- UPDATE: 新信息是对旧信息的更新或修正（合并保留细节）
- OVERWRITE: 新信息完全替代旧信息（用户明确否定旧偏好）
- NOOP: 新信息与旧信息一致或重复
- DELETE: 新信息表示旧信息已失效
- CLEAR: 用户请求清除相关记忆

只回答选项名称，不要解释。
"""
```

### 2.3 Resolution处理逻辑

```python
def _apply_resolution(self, result: ConflictResolution) -> Optional[MemoryItem]:
    """Apply resolution and return the item to store."""
    if result.operation == MemoryOperation.ADD:
        return result.new_item

    elif result.operation == MemoryOperation.UPDATE:
        # Merge: keep existing metadata, update content
        if result.existing_item and result.new_item:
            merged = result.existing_item
            merged.content = result.new_item.content
            merged.metadata.update(result.new_item.metadata or {})
            return merged
        return result.new_item

    elif result.operation == MemoryOperation.OVERWRITE:
        # 完全替换：删除旧记忆，返回新记忆（不merge）
        # 上层需要先删除existing_item，再add new_item
        return result.new_item

    elif result.operation == MemoryOperation.DELETE:
        # 单条删除：返回None表示不存储
        return None

    elif result.operation == MemoryOperation.CLEAR:
        # 批量清除：返回特殊标记，上层按类型清除
        # 体现在ConflictResolution.clear_type字段
        return None

    else:  # NOOP
        return result.existing_item
```

### 2.4 ConflictResolution数据结构扩展

```python
@dataclass
class ConflictResolution:
    """Conflict resolution result."""
    operation: MemoryOperation = MemoryOperation.ADD
    existing_item: Optional[MemoryItem] = None
    new_item: Optional[MemoryItem] = None
    reason: str = ""
    similarity: float = 0.0
    fallback_used: bool = False
    has_conflict: bool = False
    resolved_item: Optional[MemoryItem] = None
    # v2.3新增：CLEAR操作的类型范围
    clear_type: Optional[MemoryType] = None
```

### 2.5 触发场景示例

| 场景 | 用户输入 | 旧记忆 | LLM判定 |
|------|---------|--------|--------|
| 预算更新 | "预算改为5000元" | "预算2000元" | UPDATE |
| 喜好否定 | "我不喜欢博物馆了" | "喜欢博物馆" | OVERWRITE |
| 信息失效 | "不去北京了，改成上海" | "目的地北京" | DELETE |
| 清除偏好 | "忘掉我之前的偏好" | (多条preference) | CLEAR |
| 重复陈述 | "我喜欢自然风光" | "喜欢自然风光" | NOOP |

---

## 3. 测试覆盖

### 3.1 JSONL备份测试

**新建 `tests/core/memory/test_semantic_backup.py`：**

```python
import pytest
import os
import tempfile
from app.core.memory.semantic_backup import SemanticJSONLBackup, SemanticBackupConfig
from app.core.memory.hierarchy import MemoryItem, MemoryLevel, MemoryType

def test_append_and_read():
    """Test writing and reading from JSONL backup."""
    with tempfile.NamedTemporaryFile(suffix=".jsonl", delete=False) as f:
        config = SemanticBackupConfig(backup_path=f.name)
        backup = SemanticJSONLBackup(config)

        # Write two memories
        mem1 = MemoryItem(
            content="喜欢自然风光",
            level=MemoryLevel.SEMANTIC,
            memory_type=MemoryType.PREFERENCE,
            importance=0.8,
        )
        mem2 = MemoryItem(
            content="预算5000元",
            level=MemoryLevel.SEMANTIC,
            memory_type=MemoryType.CONSTRAINT,
            importance=0.9,
        )

        assert backup.append(mem1) is True
        assert backup.append(mem2) is True

        # Read back
        memories = backup.read_all()
        assert len(memories) == 2
        assert memories[0].content == "喜欢自然风光"
        assert memories[1].memory_type == MemoryType.CONSTRAINT

        os.unlink(f.name)

def test_disabled_backup():
    """Test that disabled backup returns True without writing."""
    config = SemanticBackupConfig(enabled=False)
    backup = SemanticJSONLBackup(config)

    mem = MemoryItem(content="test", level=MemoryLevel.SEMANTIC)
    assert backup.append(mem) is True

def test_get_stats():
    """Test backup file statistics."""
    with tempfile.NamedTemporaryFile(suffix=".jsonl", delete=False) as f:
        config = SemanticBackupConfig(backup_path=f.name)
        backup = SemanticJSONLBackup(config)

        # Empty file stats
        stats = backup.get_stats()
        assert stats["exists"] is True
        assert stats["count"] == 0

        # Write one memory
        backup.append(MemoryItem(content="test", level=MemoryLevel.SEMANTIC))
        stats = backup.get_stats()
        assert stats["count"] == 1

        os.unlink(f.name)
```

### 3.2 冲突策略测试

**新建 `tests/core/memory/test_conflict_resolver_v2.py`：**

```python
import pytest
from unittest.mock import AsyncMock
from app.core.memory.conflict_resolver import (
    MemoryConflictResolver,
    MemoryOperation,
    ConflictResolution,
)
from app.core.memory.hierarchy import MemoryItem, MemoryLevel, MemoryType

@pytest.mark.asyncio
async def test_overwrite_operation():
    """Test OVERWRITE operation - complete replacement."""
    resolver = MemoryConflictResolver(
        embedding_client=AsyncMock(),
        llm_client=AsyncMock()
    )
    resolver._llm.generate.return_value = "OVERWRITE"

    new_item = MemoryItem(
        content="我不喜欢博物馆了",
        level=MemoryLevel.SEMANTIC,
        memory_type=MemoryType.PREFERENCE,
    )
    existing = MemoryItem(
        content="喜欢博物馆",
        level=MemoryLevel.SEMANTIC,
        memory_type=MemoryType.PREFERENCE,
        metadata={"old_data": "value"},
    )

    # Mock high similarity
    resolver._compute_similarity_safe = AsyncMock(return_value=0.90)

    result = await resolver.resolve(new_item, [existing])

    assert result.operation == MemoryOperation.OVERWRITE
    assert result.resolved_item.content == "我不喜欢博物馆了"
    # OVERWRITE不保留旧metadata
    assert "old_data" not in result.resolved_item.metadata

@pytest.mark.asyncio
async def test_clear_operation():
    """Test CLEAR operation - batch clear by type."""
    resolver = MemoryConflictResolver(
        embedding_client=AsyncMock(),
        llm_client=AsyncMock()
    )
    resolver._llm.generate.return_value = "CLEAR"

    new_item = MemoryItem(
        content="忘掉我之前的偏好",
        level=MemoryLevel.SEMANTIC,
    )
    existing = MemoryItem(
        content="喜欢博物馆",
        memory_type=MemoryType.PREFERENCE,
    )

    resolver._compute_similarity_safe = AsyncMock(return_value=0.85)

    result = await resolver.resolve(new_item, [existing])

    assert result.operation == MemoryOperation.CLEAR
    assert result.clear_type == MemoryType.PREFERENCE
```

---

## 4. 文件变更清单

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `backend/app/core/memory/semantic_backup.py` | 新建 | JSONL备份类 |
| `backend/app/core/memory/conflict_resolver.py` | 修改 | 增加OVERWRITE/CLEAR策略 |
| `backend/app/core/memory/config.py` | 修改 | 增加JSONL配置项 |
| `backend/app/core/memory/__init__.py` | 修改 | 导出新类 |
| `backend/app/memory/semantic.py` | 修改 | 集成JSONL备份 |
| `tests/core/memory/test_semantic_backup.py` | 新建 | JSONL测试 |
| `tests/core/memory/test_conflict_resolver_v2.py` | 新建 | 冲突策略测试 |

---

## 5. 实现优先级

1. **Task 1**: JSONL备份类实现（独立模块，无依赖）
2. **Task 2**: 配置扩展（Task 1依赖）
3. **Task 3**: 集成到语义记忆写入（Task 1, 2依赖）
4. **Task 4**: 冲突策略扩展（独立模块）
5. **Task 5**: 测试覆盖（Task 1-4依赖）

---

## 6. 验收标准

完成所有任务后，应满足：

1. **JSONL备份**: 每条语义记忆写入后同步备份，失败不阻塞
2. **备份恢复**: `read_all()`可读取全部备份用于恢复
3. **OVERWRITE**: LLM返回OVERWRITE时，旧记忆完全删除，新记忆独立创建
4. **CLEAR**: LLM返回CLEAR时，返回清除类型，上层批量删除
5. **策略完整**: 6种策略（ADD/UPDATE/OVERWRITE/DELETE/CLEAR/NOOP）覆盖所有场景
6. **测试覆盖**: 新增功能有对应单元测试