# Memory v2.3 JSONL Backup and Conflict Strategies Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 补齐记忆管理系统缺失的两项功能：JSONL持久化备份 + OVERWRITE/CLEAR冲突策略

**Architecture:**
- JSONL备份：`SemanticJSONLBackup`类，同步append到文件，失败只warning不阻塞
- 冲突策略：扩展`MemoryOperation`枚举，新增OVERWRITE和CLEAR，LLM响应解析支持`CLEAR:类型`格式
- 集成点：`hierarchy.py`的`add_semantic()`触发JSONL备份，`add_semantic_with_conflict_check()`处理OVERWRITE/CLEAR

**Tech Stack:** Python 3.10+, dataclasses, asyncio, pytest

---

## File Structure Map

```
backend/app/core/memory/
├── semantic_backup.py          # 新建：JSONL备份类
├── conflict_resolver.py        # 修改：增加OVERWRITE/CLEAR + _parse_llm_response
├── config.py                   # 修改：增加jsonl_backup_enabled/path配置
├── hierarchy.py                # 修改：add_semantic()集成JSONL，add_semantic_with_conflict_check()处理新策略
└── __init__.py                 # 修改：导出SemanticJSONLBackup/SemanticBackupConfig

backend/tests/core/memory/
├── test_semantic_backup.py     # 新建：JSONL备份测试
└── test_conflict_resolver.py   # 修改：追加OVERWRITE/CLEAR测试
```

---

## Task 1: JSONL备份类实现

**Files:**
- Create: `backend/app/core/memory/semantic_backup.py`
- Create: `backend/tests/core/memory/test_semantic_backup.py`

- [ ] **Step 1: 写JSONL备份失败的测试**

```python
# backend/tests/core/memory/test_semantic_backup.py

import pytest
import os
import tempfile
import json
from app.core.memory.semantic_backup import SemanticJSONLBackup, SemanticBackupConfig
from app.core.memory.hierarchy import MemoryItem, MemoryLevel, MemoryType


def test_append_to_invalid_path_returns_false():
    """Test that append returns False when path is invalid."""
    config = SemanticBackupConfig(backup_path="/invalid/path/that/does/not/exist/file.jsonl")
    backup = SemanticJSONLBackup(config)

    mem = MemoryItem(content="test", level=MemoryLevel.SEMANTIC)

    # Should return False but not raise exception
    result = backup.append(mem)
    assert result is False
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && pytest tests/core/memory/test_semantic_backup.py::test_append_to_invalid_path_returns_false -v`
Expected: FAIL with "ModuleNotFoundError: No module named 'app.core.memory.semantic_backup'"

- [ ] **Step 3: 创建semantic_backup.py核心类**

```python
# backend/app/core/memory/semantic_backup.py

"""JSONL backup for semantic memories.

Phase 2.3: 语义记忆持久化备份

Writes each semantic memory to a JSONL file as backup.
Synchronous write, non-blocking on failure (only warning).
"""

import json
import logging
import os
from dataclasses import dataclass
from pathlib import Path
from typing import List, TYPE_CHECKING

if TYPE_CHECKING:
    from app.core.memory.hierarchy import MemoryItem

logger = logging.getLogger(__name__)


@dataclass
class SemanticBackupConfig:
    """JSONL backup configuration."""
    enabled: bool = True
    backup_path: str = "./data/semantic_backup.jsonl"
    max_file_size_mb: float = 100.0


class SemanticJSONLBackup:
    """JSONL backup for semantic memories.

    Writes each semantic memory to a JSONL file as backup.
    Synchronous write, non-blocking on failure (only warning).
    """

    def __init__(self, config: SemanticBackupConfig):
        self._config = config
        self._ensure_directory()

    def _ensure_directory(self) -> None:
        """Ensure backup directory exists."""
        if not self._config.enabled:
            return

        try:
            path = Path(self._config.backup_path)
            path.parent.mkdir(parents=True, exist_ok=True)
        except Exception as e:
            logger.warning(f"[SemanticBackup] Cannot create directory: {e}")

    def append(self, memory: "MemoryItem") -> bool:
        """Append a memory to JSONL file.

        Args:
            memory: MemoryItem to backup

        Returns:
            True if written successfully, False otherwise
        """
        if not self._config.enabled:
            return True

        try:
            line = json.dumps(memory.to_dict(), ensure_ascii=False)
            with open(self._config.backup_path, "a", encoding="utf-8") as f:
                f.write(line + "\n")
            return True
        except Exception as e:
            logger.warning(f"[SemanticBackup] Write failed: {e}")
            return False

    def read_all(self) -> List["MemoryItem"]:
        """Read all memories from backup file.

        Used for recovery when ChromaDB data lost.
        """
        from app.core.memory.hierarchy import MemoryItem

        if not os.path.exists(self._config.backup_path):
            return []

        memories = []
        skipped_count = 0
        with open(self._config.backup_path, "r", encoding="utf-8") as f:
            for line_num, line in enumerate(f, 1):
                try:
                    data = json.loads(line.strip())
                    memories.append(MemoryItem.from_dict(data))
                except json.JSONDecodeError as e:
                    skipped_count += 1
                    logger.warning(
                        f"[SemanticBackup] Skip corrupted line {line_num}: {str(e)[:50]}"
                    )

        if skipped_count > 0:
            logger.warning(
                f"[SemanticBackup] Recovery complete | "
                f"loaded={len(memories)} | skipped={skipped_count}"
            )

        return memories

    def get_stats(self) -> dict:
        """Get backup file statistics."""
        if not os.path.exists(self._config.backup_path):
            return {"exists": False, "count": 0}

        size_mb = os.path.getsize(self._config.backup_path) / (1024 * 1024)
        count = sum(1 for _ in open(self._config.backup_path, "r", encoding="utf-8"))
        return {
            "exists": True,
            "path": self._config.backup_path,
            "size_mb": round(size_mb, 2),
            "count": count,
            "max_size_mb": self._config.max_file_size_mb,
        }
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd backend && pytest tests/core/memory/test_semantic_backup.py::test_append_to_invalid_path_returns_false -v`
Expected: PASS

- [ ] **Step 5: 写正常写入和读取的测试**

```python
# backend/tests/core/memory/test_semantic_backup.py - 追加

def test_append_and_read():
    """Test writing and reading from JSONL backup."""
    with tempfile.NamedTemporaryFile(suffix=".jsonl", delete=False) as f:
        config = SemanticBackupConfig(backup_path=f.name)
        backup = SemanticJSONLBackup(config)

        # Write two memories
        mem1 = MemoryItem(
            content="喜欢自然风光",
            level=MemoryLevel.SEMANTIC,
            memory_type=MemoryType.PREFERENCE,
            importance=0.8,
        )
        mem2 = MemoryItem(
            content="预算5000元",
            level=MemoryLevel.SEMANTIC,
            memory_type=MemoryType.CONSTRAINT,
            importance=0.9,
        )

        assert backup.append(mem1) is True
        assert backup.append(mem2) is True

        # Read back
        memories = backup.read_all()
        assert len(memories) == 2
        assert memories[0].content == "喜欢自然风光"
        assert memories[1].memory_type == MemoryType.CONSTRAINT

        os.unlink(f.name)


def test_disabled_backup():
    """Test that disabled backup returns True without writing."""
    with tempfile.NamedTemporaryFile(suffix=".jsonl", delete=False) as f:
        config = SemanticBackupConfig(enabled=False, backup_path=f.name)
        backup = SemanticJSONLBackup(config)

        mem = MemoryItem(content="test", level=MemoryLevel.SEMANTIC)
        assert backup.append(mem) is True

        # File should be empty or not exist
        if os.path.exists(f.name):
            with open(f.name, "r") as file:
                content = file.read()
            assert content == "" or len(content) == 0

        os.unlink(f.name)


def test_get_stats():
    """Test backup file statistics."""
    with tempfile.NamedTemporaryFile(suffix=".jsonl", delete=False) as f:
        config = SemanticBackupConfig(backup_path=f.name)
        backup = SemanticJSONLBackup(config)

        # Empty file stats
        stats = backup.get_stats()
        assert stats["exists"] is True
        assert stats["count"] == 0

        # Write one memory
        backup.append(MemoryItem(content="test", level=MemoryLevel.SEMANTIC))
        stats = backup.get_stats()
        assert stats["count"] == 1

        os.unlink(f.name)
```

- [ ] **Step 6: 运行所有JSONL备份测试**

Run: `cd backend && pytest tests/core/memory/test_semantic_backup.py -v`
Expected: 4 tests PASS

- [ ] **Step 7: 提交Task 1**

```bash
git add backend/app/core/memory/semantic_backup.py
git add backend/tests/core/memory/test_semantic_backup.py
git commit -m "feat(memory): add SemanticJSONLBackup for semantic memory backup

- SemanticBackupConfig dataclass for configuration
- SemanticJSONLBackup class with append/read_all/get_stats
- Non-blocking write (warning on failure)
- Recovery support with corrupted line skip logging"
```

---

## Task 2: 配置扩展

**Files:**
- Modify: `backend/app/core/memory/config.py` (lines 85-153)

- [ ] **Step 1: 添加JSONL配置项到MemoryConfig**

```python
# backend/app/core/memory/config.py - 在 MemoryConfig 类中添加 (约line 153之后)

    # ---- JSONL Backup v2.3 Configuration ----

    jsonl_backup_enabled: bool = True
    jsonl_backup_path: str = "./data/semantic_backup.jsonl"
    jsonl_max_file_size_mb: float = 100.0
```

- [ ] **Step 2: 运行现有测试确保无破坏**

Run: `cd backend && pytest tests/core/memory/ -v -k "config"`
Expected: 所有相关测试PASS

- [ ] **Step 3: 提交Task 2**

```bash
git add backend/app/core/memory/config.py
git commit -m "feat(memory): add JSONL backup configuration to MemoryConfig

- jsonl_backup_enabled: toggle for backup feature
- jsonl_backup_path: file path for backup
- jsonl_max_file_size_mb: max file size before rotate"
```

---

## Task 3: 冲突策略扩展

**Files:**
- Modify: `backend/app/core/memory/conflict_resolver.py`
- Modify: `backend/tests/core/memory/test_conflict_resolver.py`

- [ ] **Step 1: 写OVERWRITE策略测试**

```python
# backend/tests/core/memory/test_conflict_resolver.py - 追加

import pytest
from unittest.mock import AsyncMock
from app.core.memory.conflict_resolver import (
    MemoryConflictResolver,
    MemoryOperation,
    ConflictResolution,
)
from app.core.memory.hierarchy import MemoryItem, MemoryLevel, MemoryType


@pytest.mark.asyncio
async def test_overwrite_operation():
    """Test OVERWRITE operation - complete replacement."""
    async def generate_overwrite(prompt):
        return "OVERWRITE"

    embedding_client = AsyncMock()
    embedding_client.embed_query = lambda text: [0.1] * 384

    llm_client = AsyncMock()
    llm_client.generate = generate_overwrite

    resolver = MemoryConflictResolver(
        embedding_client=embedding_client,
        llm_client=llm_client
    )

    new_item = MemoryItem(
        content="我不喜欢博物馆了",
        level=MemoryLevel.SEMANTIC,
        memory_type=MemoryType.PREFERENCE,
    )
    existing = MemoryItem(
        content="喜欢博物馆",
        level=MemoryLevel.SEMANTIC,
        memory_type=MemoryType.PREFERENCE,
        metadata={"old_data": "value"},
    )

    # 注意：_compute_similarity_safe内部使用run_in_executor包装同步函数
    # 所以mock必须是同步函数，不能是async def
    def mock_similarity(item1, item2):
        return 0.90
    resolver._compute_similarity_safe = mock_similarity

    result = await resolver.resolve(new_item, [existing])

    assert result.operation == MemoryOperation.OVERWRITE
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && pytest tests/core/memory/test_conflict_resolver.py::test_overwrite_operation -v`
Expected: FAIL - OVERWRITE not in MemoryOperation enum

- [ ] **Step 3: 扩展MemoryOperation枚举**

```python
# backend/app/core/memory/conflict_resolver.py - 修改 MemoryOperation 枚举 (约line 16-22)

class MemoryOperation(Enum):
    """Memory operation types."""
    ADD = "add"
    UPDATE = "update"
    DELETE = "delete"
    NOOP = "noop"
    OVERWRITE = "overwrite"  # v2.3新增：完全替换
    CLEAR = "clear"          # v2.3新增：批量清除
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd backend && pytest tests/core/memory/test_conflict_resolver.py::test_overwrite_operation -v`
Expected: PASS

- [ ] **Step 5: 扩展ConflictResolution数据结构**

```python
# backend/app/core/memory/conflict_resolver.py - 修改 ConflictResolution dataclass (约line 25-36)

@dataclass
class ConflictResolution:
    """Conflict resolution result (unified interface for v2.1)."""
    operation: MemoryOperation = MemoryOperation.ADD
    existing_item: Optional["MemoryItem"] = None
    new_item: Optional["MemoryItem"] = None
    reason: str = ""
    similarity: float = 0.0
    fallback_used: bool = False
    # v2.1 additional fields for hierarchy.py compatibility
    has_conflict: bool = False
    resolved_item: Optional["MemoryItem"] = None
    # v2.3新增：CLEAR操作的类型范围
    clear_type: Optional[MemoryType] = None
```

- [ ] **Step 6: 添加LLM响应解析方法**

```python
# backend/app/core/memory/conflict_resolver.py - 在类中添加新方法

    def _parse_llm_response(
        self,
        response: str,
        existing_item: "MemoryItem"
    ) -> tuple[MemoryOperation, Optional[MemoryType]]:
        """Parse LLM response to get operation and optional clear type.

        v2.3新增：支持OVERWRITE和CLEAR:类型格式

        Returns:
            (operation, clear_type) tuple
        """
        response = response.strip().upper()

        # CLEAR response may include type: "CLEAR:PREFERENCE" or just "CLEAR"
        if response.startswith("CLEAR"):
            parts = response.split(":")
            if len(parts) > 1:
                try:
                    clear_type = MemoryType(parts[1].lower())
                except ValueError:
                    clear_type = existing_item.memory_type
            else:
                clear_type = existing_item.memory_type
            return (MemoryOperation.CLEAR, clear_type)

        # Other operations
        operation_map = {
            "UPDATE": MemoryOperation.UPDATE,
            "OVERWRITE": MemoryOperation.OVERWRITE,
            "DELETE": MemoryOperation.DELETE,
            "NOOP": MemoryOperation.NOOP,
            "ADD": MemoryOperation.ADD,
        }

        first_word = response.split()[0] if response.split() else response
        return (operation_map.get(first_word, MemoryOperation.NOOP), None)
```

- [ ] **Step 7: 更新LLM Prompt以包含新选项**

```python
# backend/app/core/memory/conflict_resolver.py - 修改 _llm_confirm_operation_safe 方法中的prompt (约line 140-152)

    async def _llm_confirm_operation_safe(
        self,
        new_item: "MemoryItem",
        existing_item: "MemoryItem"
    ) -> MemoryOperation:
        """LLM confirmation with timeout and error handling."""
        prompt = f"""
判断以下两句话的关系：

旧信息: {existing_item.content}
新信息: {new_item.content}
旧信息类型: {existing_item.memory_type.value if existing_item.memory_type else 'unknown'}

请回答以下选项之一：
- UPDATE: 新信息是对旧信��的更新或修正
- OVERWRITE: 新信息完全替代旧信息（用户明确否定旧偏好）
- NOOP: 新信息与旧信息一致或重复
- DELETE: 新信息表示旧信息已失效
- CLEAR: 用户请求清除相关记忆（回答格式: CLEAR:类型，如CLEAR:preference）

只回答选项名称，不要解释。
"""

        try:
            response = await asyncio.wait_for(
                self._llm.generate(prompt),
                timeout=5.0
            )

            # v2.3: 使用新的解析方法
            operation, clear_type = self._parse_llm_response(response, existing_item)
            return operation

        except (asyncio.TimeoutError, Exception) as e:
            raise Exception(f"LLM确认失败: {e}")
```

- [ ] **Step 8: 更新_apply_resolution方法**

```python
# backend/app/core/memory/conflict_resolver.py - 修改 _apply_resolution 方法 (约line 67-82)

    def _apply_resolution(self, result: ConflictResolution) -> Optional["MemoryItem"]:
        """Apply resolution and return the item to store."""
        if result.operation == MemoryOperation.ADD:
            return result.new_item
        elif result.operation == MemoryOperation.UPDATE:
            # Merge: keep existing metadata, update content
            if result.existing_item and result.new_item:
                merged = result.existing_item
                merged.content = result.new_item.content
                merged.metadata.update(result.new_item.metadata or {})
                return merged
            return result.new_item
        elif result.operation == MemoryOperation.OVERWRITE:
            # v2.3: 完全替换，返回新记忆（上层删除旧记忆）
            return result.new_item
        elif result.operation == MemoryOperation.DELETE:
            return None
        elif result.operation == MemoryOperation.CLEAR:
            # v2.3: 批量清除，返回None（上层按clear_type清除）
            return None
        else:  # NOOP
            return result.existing_item
```

- [ ] **Step 9: 更新resolve方法使用新解析**

```python
# backend/app/core/memory/conflict_resolver.py - 修改 resolve 方法 (约line 84-114)

    async def resolve(
        self,
        new_memory: "MemoryItem",
        existing_memories: List["MemoryItem"],
    ) -> ConflictResolution:
        """Resolve memory conflict."""
        for existing in existing_memories:
            similarity = await self._compute_similarity_safe(new_memory, existing)

            if similarity >= self.SEMANTIC_THRESHOLD:
                try:
                    operation = await self._llm_confirm_operation_safe(
                        new_memory, existing
                    )

                    # v2.3: 解析可能包含clear_type
                    if operation == MemoryOperation.CLEAR:
                        _, clear_type = self._parse_llm_response("CLEAR", existing)
                        return ConflictResolution(
                            operation=operation,
                            existing_item=existing,
                            new_item=new_memory,
                            similarity=similarity,
                            reason=f"相似度{similarity:.2f}，LLM确认为CLEAR",
                            clear_type=clear_type,
                        )

                    return ConflictResolution(
                        operation=operation,
                        existing_item=existing,
                        new_item=new_memory,
                        similarity=similarity,
                        reason=f"相似度{similarity:.2f}，LLM确认为{operation.value}"
                    )
                except Exception as e:
                    logger.warning(f"[ConflictResolver] LLM确认失败: {e}")
                    return self._fallback_resolution(new_memory, existing, similarity)

        return ConflictResolution(
            operation=MemoryOperation.ADD,
            new_item=new_memory,
            reason="全新信息，无冲突"
        )
```

- [ ] **Step 10: 写CLEAR策略测试**

```python
# backend/tests/core/memory/test_conflict_resolver.py - 追加

@pytest.mark.asyncio
async def test_clear_operation_with_type():
    """Test CLEAR operation - LLM returns CLEAR:PREFERENCE."""
    async def generate_clear_with_type(prompt):
        return "CLEAR:PREFERENCE"

    embedding_client = AsyncMock()
    embedding_client.embed_query = lambda text: [0.1] * 384

    llm_client = AsyncMock()
    llm_client.generate = generate_clear_with_type

    resolver = MemoryConflictResolver(
        embedding_client=embedding_client,
        llm_client=llm_client
    )

    new_item = MemoryItem(
        content="忘掉我之前的偏好",
        level=MemoryLevel.SEMANTIC,
    )
    existing = MemoryItem(
        content="喜欢博物馆",
        level=MemoryLevel.SEMANTIC,
        memory_type=MemoryType.PREFERENCE,
    )

    # 注意：mock必须是同步函数（见test_overwrite_operation注释）
    def mock_similarity(item1, item2):
        return 0.85
    resolver._compute_similarity_safe = mock_similarity

    result = await resolver.resolve(new_item, [existing])

    assert result.operation == MemoryOperation.CLEAR
    assert result.clear_type == MemoryType.PREFERENCE


@pytest.mark.asyncio
async def test_clear_operation_without_type():
    """Test CLEAR operation - LLM returns just CLEAR, fallback to existing type."""
    async def generate_clear_no_type(prompt):
        return "CLEAR"

    embedding_client = AsyncMock()
    embedding_client.embed_query = lambda text: [0.1] * 384

    llm_client = AsyncMock()
    llm_client.generate = generate_clear_no_type

    resolver = MemoryConflictResolver(
        embedding_client=embedding_client,
        llm_client=llm_client
    )

    new_item = MemoryItem(
        content="忘掉之前的设置",
        level=MemoryLevel.SEMANTIC,
    )
    existing = MemoryItem(
        content="预算5000元",
        level=MemoryLevel.SEMANTIC,
        memory_type=MemoryType.CONSTRAINT,
    )

    def mock_similarity(item1, item2):
        return 0.85
    resolver._compute_similarity_safe = mock_similarity

    result = await resolver.resolve(new_item, [existing])

    assert result.operation == MemoryOperation.CLEAR
    assert result.clear_type == MemoryType.CONSTRAINT
```

- [ ] **Step 11: 运行所有冲突策略测试**

Run: `cd backend && pytest tests/core/memory/test_conflict_resolver.py -v`
Expected: 所有测试PASS，包括新增的OVERWRITE和CLEAR测试

- [ ] **Step 12: 提交Task 3**

```bash
git add backend/app/core/memory/conflict_resolver.py
git add backend/tests/core/memory/test_conflict_resolver.py
git commit -m "feat(memory): add OVERWRITE and CLEAR conflict resolution strategies

- Extend MemoryOperation enum with OVERWRITE and CLEAR
- Add clear_type field to ConflictResolution
- Add _parse_llm_response for CLEAR:type format
- Update LLM prompt to include new options
- Tests for OVERWRITE, CLEAR with type, CLEAR without type"
```

---

## Task 4: Hierarchy集成

**Files:**
- Modify: `backend/app/core/memory/hierarchy.py`

- [ ] **Step 1: 添加jsonl_backup和conflict_resolver参数到__init__**

```python
# backend/app/core/memory/hierarchy.py - 修改 __init__ 方法

# 首先添加导入（约line 18-19）
if TYPE_CHECKING:
    from app.core.memory.conflict_resolver import MemoryConflictResolver, ConflictResolution, MemoryOperation
    from app.core.memory.ttl_manager import TTLMemoryManager, CleanupStats
    from app.core.memory.redis_episodic import RedisEpisodicStore
    from app.core.memory.semantic_backup import SemanticJSONLBackup  # v2.3新增

# 修改 __init__ 参数列表（约line 131-139）
    def __init__(
        self,
        working_max_size: int = 20,
        working_max_tokens: int = 4000,
        conversation_id: Optional[UUID] = None,
        user_id: Optional[str] = None,
        compact_mode: bool = False,
        redis_store: Optional["RedisEpisodicStore"] = None,
        jsonl_backup: Optional["SemanticJSONLBackup"] = None,  # v2.3新增
        conflict_resolver: Optional["MemoryConflictResolver"] = None,  # v2.3新增：允许注入resolver
    ):
        """Initialize memory hierarchy.

        Args:
            working_max_size: Maximum number of items in working memory
            working_max_tokens: Maximum tokens in working memory
            conversation_id: Optional conversation ID for episodic memory
            user_id: Optional user ID for semantic memory
            compact_mode: If True, reduces working memory limits
            redis_store: Optional Redis store for episodic memory persistence
            jsonl_backup: Optional JSONL backup for semantic memory (v2.3)
            conflict_resolver: Optional conflict resolver for semantic memory (v2.3)
        """
        # ... 现有初始化代码 ...
        self._conflict_resolver = conflict_resolver  # v2.3修改：从参数注入
        self._jsonl_backup = jsonl_backup  # v2.3新增
```

- [ ] **Step 2: 修改add_semantic集成JSONL备份**

```python
# backend/app/core/memory/hierarchy.py - 修改 add_semantic 方法 (约line 216-226)

    def add_semantic(self, item: MemoryItem) -> None:
        """Add a semantic memory."""
        if item.level != MemoryLevel.SEMANTIC:
            item.level = MemoryLevel.SEMANTIC

        self._semantic.append(item)

        # v2.3新增：同步JSONL备份
        if self._jsonl_backup:
            self._jsonl_backup.append(item)

        logger.debug(f"[MemoryHierarchy] Added semantic: {item.memory_type} - {item.content[:50]}")
```

- [ ] **Step 3: 修改add_semantic_with_conflict_check处理OVERWRITE和CLEAR**

```python
# backend/app/core/memory/hierarchy.py - 修改 add_semantic_with_conflict_check 方法 (约line 228-266)

    async def add_semantic_with_conflict_check(
        self,
        item: MemoryItem,
        resolve: bool = True,
    ) -> "ConflictResolution":
        """Add a semantic memory with conflict detection and optional resolution.

        v2.3新增：处理OVERWRITE和CLEAR操作
        """
        if item.level != MemoryLevel.SEMANTIC:
            item.level = MemoryLevel.SEMANTIC

        async with self._semantic_lock:
            if self._conflict_resolver:
                conflict_result = await self._conflict_resolver.check_conflict(
                    item, self._semantic
                )
                if conflict_result.has_conflict and resolve:
                    # v2.3: 根据operation类型执行不同操作
                    if conflict_result.operation == MemoryOperation.OVERWRITE:
                        # OVERWRITE: 先删除旧记忆，再添加新记忆
                        if conflict_result.existing_item:
                            self._semantic = [
                                m for m in self._semantic
                                if m.item_id != conflict_result.existing_item.item_id
                            ]
                            logger.info(
                                f"[MemoryHierarchy] OVERWRITE: removed {conflict_result.existing_item.item_id}"
                            )
                        # 添加新记忆（不merge旧metadata）
                        new_item = MemoryItem(
                            content=item.content,
                            level=MemoryLevel.SEMANTIC,
                            memory_type=item.memory_type,
                            metadata=item.metadata or {},
                            importance=item.importance,
                        )
                        self._semantic.append(new_item)
                        # JSONL备份
                        if self._jsonl_backup:
                            self._jsonl_backup.append(new_item)
                        conflict_result.resolved_item = new_item

                    elif conflict_result.operation == MemoryOperation.CLEAR:
                        # CLEAR: 按类型批量清除
                        clear_type = conflict_result.clear_type
                        if clear_type:
                            removed_count = len([
                                m for m in self._semantic
                                if m.memory_type == clear_type
                            ])
                            self._semantic = [
                                m for m in self._semantic
                                if m.memory_type != clear_type
                            ]
                            logger.info(
                                f"[MemoryHierarchy] CLEAR: removed {removed_count} memories of type {clear_type}"
                            )
                        conflict_result.resolved_item = None

                    else:
                        # 其他操作（UPDATE/DELETE/NOOP）由resolver处理
                        resolved_item = self._conflict_resolver._apply_resolution(conflict_result)
                        if resolved_item:
                            self._semantic.append(resolved_item)
                            # JSONL备份
                            if self._jsonl_backup:
                                self._jsonl_backup.append(resolved_item)

                    return conflict_result

            # 无冲突：直接添加
            self._semantic.append(item)
            # JSONL备份
            if self._jsonl_backup:
                self._jsonl_backup.append(item)

            from app.core.memory.conflict_resolver import ConflictResolution
            return ConflictResolution(has_conflict=False, resolved_item=item)
```

- [ ] **Step 4: 运行现有hierarchy测试确保无破坏**

Run: `cd backend && pytest tests/core/ -v -k "hierarchy"`
Expected: 所有相关测试PASS

- [ ] **Step 5: 提交Task 4**

```bash
git add backend/app/core/memory/hierarchy.py
git commit -m "feat(memory): integrate JSONL backup and OVERWRITE/CLEAR in MemoryHierarchy

- Add jsonl_backup parameter to __init__
- Call jsonl_backup.append() in add_semantic()
- Handle OVERWRITE (delete old, add new)
- Handle CLEAR (batch remove by type)
- JSONL backup for all semantic additions"
```

---

## Task 5: 导出更新

**Files:**
- Modify: `backend/app/core/memory/__init__.py`

- [ ] **Step 1: 添加新导出**

```python
# backend/app/core/memory/__init__.py - 添加导��和导出

from .semantic_backup import SemanticJSONLBackup, SemanticBackupConfig

# 在 __all__ 列表中添加
__all__ = [
    # ... 现有导出 ...
    # JSONL Backup v2.3
    "SemanticJSONLBackup",
    "SemanticBackupConfig",
]
```

- [ ] **Step 2: 运行导入测试**

Run: `cd backend && python -c "from app.core.memory import SemanticJSONLBackup, SemanticBackupConfig; print('OK')"`
Expected: 输出 "OK"

- [ ] **Step 3: 提交Task 5**

```bash
git add backend/app/core/memory/__init__.py
git commit -m "feat(memory): export SemanticJSONLBackup and SemanticBackupConfig"
```

---

## Task 6: 验证测试

- [ ] **Step 1: 运行完整测试套件**

Run: `cd backend && pytest tests/core/memory/ -v`
Expected: 所有测试PASS

- [ ] **Step 2: 运行集成测试确认功能**

Run: `cd backend && pytest tests/core/ -v`
Expected: 所有测试PASS

- [ ] **Step 3: 最终提交（如有遗漏）**

```bash
git status
# 如有未提交的修改，提交它们
git commit -m "test(memory): ensure all v2.3 tests pass"
```

---

## 验收标准

完成所有任务后，应满足：

1. **JSONL备份**: `SemanticJSONLBackup.append()` 写入成功返回True，失败返回False不阻塞
2. **备份恢复**: `read_all()` 读取全部备份，损坏行有warning日志
3. **OVERWRITE**: LLM返回OVERWRITE时，旧记忆删除，新记忆独立创建（不merge metadata）
4. **CLEAR**: LLM返回CLEAR:PREFERENCE或CLEAR时，正确解析clear_type
5. **策略完整**: 6种策略（ADD/UPDATE/OVERWRITE/DELETE/CLEAR/NOOP）
6. **测试覆盖**: 新增4个JSONL测试 + 3个冲突策略测试